import { Box } from "@mui/material"
import { HeroSection } from "../components/HeroSection"

export const Home = () => {
    return (
        <Box sx={{
            height: '100vh',
        }}>
            <HeroSection />
        </Box>
    )
}
